{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -m db.migrate && python -m pipeline.heal",
    "restartPolicyType": "NEVER"
  }
}
